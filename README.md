

    Kobo eReader (built-in web browser used as the always-on display)

    A static dashboard web page (dashboard.html) written in HTML/CSS/JavaScript

    A manager web page (index.html) to add/edit tasks and events

    GitHub (repository to store the project files)

    GitHub Pages (hosting for the dashboard + manager so they’re reachable online)

    Firebase Realtime Database (cloud data store for events and todos)

    Firebase Realtime Database REST API (used by the Kobo page via XMLHttpRequest to GET/PATCH/DELETE JSON)

    Open-Meteo API (weather + forecast data pulled by the dashboard)

    JavaScript polling (setInterval) to refresh data periodically

    Simple e-ink refresh “nudge” techniques (forced reflow / DOM changes to encourage screen updates)
